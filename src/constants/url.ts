export const RELATIVE_URL_BASE = 'https://placeholder.local';

export const ABSOLUTE_URL_PATTERN = /^[a-z][a-z\d+\-.]*:\/\//i;

export const URL_SCHEME_PATTERN = /^[a-z][a-z\d+\-.]*:/i;

export const HTTP_URL_PATTERN = /^https?:\/\//i;

export const PROTOCOL_RELATIVE_URL_PATTERN = /^\/\//;

export const URL_AUTHORITY_PATTERN = /:\/\//;

export const INVALID_HREF_SCHEME_PATTERN = /^(javascript|data|vbscript):/i;
